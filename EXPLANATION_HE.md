# AIGuard - מדריך מקיף והסבר טכני

## 📚 מבוא
**AIGuard** הוא פלאגין חדשני לשרתי Minecraft (Spigot/Paper) שנועד לספק הגנה חכמה ומתקדמת מפני שפה בוטה, קללות, ועקיפת צנזורה בצ'אט. בשונה מפלאגינים רגילים המסתמכים על התאמה מדויקת של מילים (Exact Match), הפלאגין AIGuard עושה שימוש באלגוריתמים מעולם הבינה המלאכותית ועיבוד השפה הטבעית (NLP) כדי לזהות וריאציות, עיוותים, וניסיונות הסוואה של מילים אסורות.

## 🧠 המנוע החכם (Fuzzy Matching Engine)
הלב של המערכת הוא מנוע ההתאמה המטושטשת (`FuzzyMatcher`). המנוע משלב שתי טכניקות מתמטיות עוצמתיות לחישוב דמיון בין טקסטים:

### 1. Levenshtein Distance (מרחק לוינשטיין)
אלגוריתם זה מחשב כמה "פעולות עריכה" (הוספה, מחיקה, או החלפה של תו) נדרשות כדי להפוך מחרוזת אחת לאחרת.
- **לדוגמה**: כדי להפוך "אפס" ל-"אפפס", נדרשת פעולה אחת (הוספת 'פ').
- המערכת מנרמלת את התוצאה לאחוזים (0% עד 100%).

### 2. Cosine Similarity (דמיון קוסינוס)
אלגוריתם זה מתייחס למילים כאל "וקטורים" במרחב רב-ממדי של תווים. הוא בודק את הזווית בין הווקטורים.
- שיטה זו יעילה מאוד לזיהוי מילים שבהן סדר האותיות שונה או שיש חזרות רבות על אותיות.
- לדוגמה: "אאאפפפססס" יקבל דירוג דמיון גבוה ל-"אפס" בשיטה זו.

### ⚖️ השילוב (Hybrid Score)
המערכת מחשבת **ציון משוקלל** המשלב את שתי השיטות הללו (עם משקל גבוה יותר ל-Levenshtein) כדי לקבל את ההחלטה המדויקת ביותר. אם הציון חוצה את **רף הדמיון** (Threshold) שהוגדר בקונפיגורציה (ברירת מחדל: 75%), המילה תזוהה כאסורה.

## 🛠️ ארכיטקטורה ומבנה
הפלאגין בנוי בצורה מודולרית ויעילה:

### 📂 מודולים עיקריים
1.  **ChatListener**: מאזין לכל הודעה שנשלחת בשרת.
    - עובד בצורה **אסינכרונית** (Async) כדי לא לפגוע בביצועי השרת (TPS).
    - מסנן צבעים ותווים מיוחדים לפני הבדיקה.
2.  **TicketManager**: מנהל את מערכת הדיווחים.
    - כל זיהוי יוצר "טיקט" (Ticket) שנשמר בזיכרון.
    - מאפשר לצוות לנהל, לקחת לטיפול (Claim), ולסגור טיקטים.
3.  **TicketGUI**: ממשק משתמש ויזואלי (Inventory GUI) לצוות.
    - צפייה בכל הטיקטים הפתוחים.
    - גישה להיסטוריית טיקטים מלאה.
    - השתגרות מהירה למקום האירוע או לשרת הרלוונטי.
4.  **BungeeManager**: (אופציונלי) מודול לתקשורת בין שרתים.
    - מאפשר קבלת התראות מכל השרתים ברשת (Network-wide Alerts).
    - משתמש ב-Plugin Messaging Channel להעברת מידע בזמן אמת.

## ⚙️ קונפיגורציה והתאמה אישית
הפלאגין גמיש לחלוטין ומאפשר למנהל השרת לשלוט בכל היבט:

### קובץ `config.yml`
- **similarity_threshold**: רגישות הזיהוי. ערך נמוך מדי יגרום לזיהויים שגויים (False Positives), ערך גבוה מדי עלול לפספס (False Negatives). המומלץ הוא 0.7-0.8.
- **banned_words**: רשימת המילים לחסימה. ניתן להוסיף מילים בכל שפה.
- **bungee**: הגדרות לרשת שרתים (הפעלה, שם השרת, ערוץ תקשורת).

### קובץ `messages.yml`
- כל ההודעות בפלאגין ניתנות לתרגום ועריכה.
- תמיכה בצבעי Minecraft (`&` או `§`).
- תמיכה במשתנים (Placeholders) כמו `{player}`, `{similarity}`, `{word}`.

## 🚀 תהליך עבודה לדוגמה
1.  שחקן כותב בצ'אט: "אתה חתיכת א פ ס!".
2.  `ChatListener` תופס את ההודעה (לפני שהיא נשלחת לכולם, או במקביל - תלוי בהגדרת הביטול).
3.  ההודעה עוברת ניקוי (הסרת רווחים מיותרים, צבעים).
4.  המנוע משווה כל מילה ברשימת ה-`banned_words`.
5.  נמצאה התאמה בין "א פ ס" ל-"אפס" עם דמיון של 85%.
6.  **פעולה**:
    - נשלחת התראה לכל אנשי הצוות המחוברים (עם הרשאת `aiwatch.alert`).
    - נוצר טיקט חדש במערכת.
    - אם מוגדר BungeeCord, ההתראה נשלחת לכל השרתים האחרים.

## 🛡️ הרשאות (Permissions)
- `aiwatch.alert`: קבלת התראות בזמן אמת.
- `aiwatch.admin`: גישה לפקודות ניהול (`reload`, וכו') ולשימוש ב-GUI.

## 📝 סיכום
AIGuard הוא לא סתם עוד פלאגין צ'אט. הוא כלי הגנה אקטיבי המשתמש בטכנולוגיה חכמה כדי לחסוך זמן לצוות הניהול ולשמור על שרת נקי ונעים יותר, תוך התמודדות עם הדרכים היצירתיות ששחקנים מוצאים כדי לעקוף חוקים.
